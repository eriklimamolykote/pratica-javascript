// Selecionando elemento
var elem = document.getElementById( "intro" );

// Obtendo informação de estilo do elemento
alert( elem.style.color );
alert( elem.style.fontSize );
alert( elem.style.fontWeight );