// Definindo função
function showFullName( firstName, lastName ) {

    document.write( firstName + " " + lastName );

}

// Chamando função
showFullName( "Erik", "Lima" ); // Imprime: Erik Lima
document.write( "<br>" );
showFullName( "Erikucho" ); // Imprime: Erikucho undefined