// Você não pode usar a classe ainda.
// MyCar = new Car("Ford")
// Isto causará um erro.

class Car {
    constructor(brand) {
        this.carname = brand;
    }
}

// Agora você pode usar a classe:
let myCar = new Car("Ford")